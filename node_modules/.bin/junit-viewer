#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../junit-viewer/bin/junit-viewer" "$@"
  ret=$?
else 
  node  "$basedir/../junit-viewer/bin/junit-viewer" "$@"
  ret=$?
fi
exit $ret
